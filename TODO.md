Features:
- Add the ability to combine audio tracks or keep separate when converting videos with multiple audio tracks (default: keep separate tracks)
- Add the ability to rename files right in the interface once converted
- Add a document previewer/viewer in a separate sidebar for text and pdf files
- Add the ability to preview videos and audio files

Future:
- PDF Editing & Creation section
- Image metadata editing
- Add a page to check which formats are supported for input/output